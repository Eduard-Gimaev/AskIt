<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= t('admin.user_mailer.bulk_import_error_email.subject') %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f9f9f9;
      margin: 0;
      padding: 0;
    }
    .container {
      width: 80%;
      max-width: 600px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    h1 {
      color: #d9534f;
      font-size: 24px;
      margin-bottom: 20px;
    }
    p {
      margin: 10px 0;
    }
    .footer {
      margin-top: 20px;
      font-size: 12px;
      color: #777;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= t('admin.user_mailer.bulk_import_error_email.title') %></h1>
    <p><%= t('admin.user_mailer.bulk_import_error_email.greeting', name: @user.name) %></p>
    <p><%= t('admin.user_mailer.bulk_import_error_email.message') %></p>
    <p><%= t('admin.user_mailer.bulk_import_error_email.error_message', error: @error_message) %></p>
    <p><%= t('admin.user_mailer.bulk_import_error_email.review_log') %></p>
    <p><%= t('admin.user_mailer.bulk_import_error_email.thank_you') %></p>
    <p><%= t('admin.user_mailer.bulk_import_error_email.signature') %></p>
    <div class="footer">
      <p><%= t('admin.user_mailer.bulk_import_error_email.footer') %></p>
    </div>
  </div>
</body>
</html>