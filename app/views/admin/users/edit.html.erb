<%= provide :title, t('actions.edit_profile') %>

<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>Edit User</h2>
    </div>
    <div class="card-body">
      <%= render 'form', user: @user %>
    </div>
  </div>
</div>